apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hello-minikube
  name: hello-minikube
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-minikube
  strategy: {}
  template:
    metadata:
      labels:
        app: hello-minikube
    spec:
      containers:
      - image: hello-minikube:latest
        name: hello-minikube
        resources: {}
status: {}
